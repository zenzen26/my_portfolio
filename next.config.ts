/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/my_portfolio',
  assetPrefix: '/my_portfolio/',
  trailingSlash: true,
};

module.exports = nextConfig;
